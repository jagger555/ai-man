# AI数字人景区导览系统实现规划

## 目标

本项目面向比赛交付，目标不是一次性做完整商业系统，而是交付一个功能完整、演示稳定、评测可解释的 AI 数字人景区导览 MVP。系统需要覆盖游客交互端和管理后台，并能清楚展示多模态交互、知识库问答、个性化推荐、数字人播报和游客洞察。

## 当前基础

仓库已经具备前后端雏形：

- 后端使用 FastAPI，已有游客问答、知识库管理、数字人配置、反馈、问答记录和游客报告相关模块。
- 前端使用 React/Vite，已有游客问答视图、数字人面板、管理后台、知识库管理和游客报告面板。
- 数据侧已有示范景区知识资料和本地问答记录数据库。
- 需要优先处理前端中文乱码、演示稳定性、评测测试集和语音/数字人链路完善。

## 总体架构

```text
游客输入
  -> 文本输入 / 语音输入
  -> 问题清洗与会话上下文
  -> 本地知识库检索
  -> 大模型导游式回答
  -> 来源引用与置信度判断
  -> TTS / 数字人播报
  -> 问答记录与反馈入库
  -> 管理后台分析与知识库改进
```

## 模块拆分

### 1. 知识库与 RAG 问答

目标：保证景区事实类问答准确、可解释、可兜底。

主要任务：

- 整理示范景区资料，按景点、历史文化、游览须知、FAQ、路线等分类。
- 改进分块、检索和排序策略，避免长文档粗糙切块导致答非所问。
- 为每次回答返回来源片段、置信度和可靠性判断。
- 对低置信度问题给出“资料不足”的导游式兜底，而不是编造。
- 建立标准测试集，覆盖事实问答、追问、无资料问题和推荐类问题。

验收方式：

- 标准事实问题准确率目标不低于 90%。
- 无资料问题不编造，能够提示资料不足或建议咨询景区工作人员。
- 每条回答在管理端可追溯 Prompt、来源和模型状态。

### 2. 游客端交互体验

目标：形成评委一眼能看懂的游客使用链路。

主要任务：

- 修复前端中文乱码，统一游客端文案和视觉风格。
- 保留文本问答作为最稳定入口。
- 增加语音输入按钮，优先采用浏览器语音识别或后端 Whisper 方案。
- 增加回答播报，优先采用浏览器 TTS 或可配置后端 TTS。
- 展示回答来源、置信度、响应时间和当前模型状态。
- 增加游客反馈按钮：有帮助 / 没有帮助 / 补充意见。

验收方式：

- 用户可以完成“输入问题 -> 获得回答 -> 听到播报 -> 提交反馈”的完整流程。
- 前端在模型失败、数字人服务不可用、知识不足时都有清晰兜底状态。
- 常见问题响应时间满足演示需要，文本问答优先控制在 5 秒以内。

### 3. 数字人集成

目标：让数字人成为真实可演示能力，而不是静态装饰。

主要任务：

- 保留当前 LiveTalking 集成方向，明确启动方式、配置项和失败兜底。
- 前端读取数字人配置，建立 WebRTC 视频流。
- 问答完成后调用数字人播报接口，使回答文本驱动口型同步。
- 管理端提供数字人基础配置：形象、声音、服务地址、启用状态。
- 演示模式下允许没有真实数字人服务时显示模拟状态和播报文本。

验收方式：

- 有数字人服务时，回答能触发视频/口型播报。
- 无数字人服务时，游客端不崩溃，能继续完成文本和语音播报演示。
- 部署手册明确数字人服务的启动命令和环境变量。

### 4. 个性化路线推荐

目标：用可控方式满足“个性化推荐”要求。

主要任务：

- 定义游客兴趣标签：历史文化、自然风光、亲子休闲、礼佛祈福、拍照打卡、半日游等。
- 建立路线模板：经典路线、文化路线、轻松路线、深度路线。
- 在问答请求中支持兴趣和时间偏好。
- 输出路线顺序、推荐理由、讲解重点和注意事项。
- 后续可接入位置/GPS，但第一版不依赖真实定位。

验收方式：

- 游客选择不同兴趣时，系统能给出不同路线和讲解侧重点。
- 推荐内容来自知识库或可维护配置，不依赖模型自由编造。
- 推荐结果可以在演示视频中稳定复现。

### 5. 管理后台

目标：展示系统不仅服务游客，也服务景区运营。

主要任务：

- 知识库管理：新增、编辑、上传、归档、搜索和分类统计。
- 问答记录：按时间、会话、关键词、置信度、模型状态筛选。
- 游客反馈：展示有帮助/无帮助、反馈文本和关联问题。
- 低置信度问题池：帮助管理员发现知识库缺口。
- 游客感受度报告：关注点、情绪趋势、常见问题、服务建议。
- 数据大屏：服务人次、热门问答、满意度趋势、平均响应时间。

验收方式：

- 管理端能解释“游客问了什么、系统答得怎么样、哪里需要补知识”。
- 后台数据来自真实问答记录，而不是纯静态展示。
- 管理端适合截图进入设计文档和 PPT。

### 6. 模型与服务配置

目标：真实模型可接入，演示模式也稳定。

主要任务：

- 保留 Mock LLM 作为离线兜底。
- 支持 OpenAI 兼容 API 配置：`LLM_PROVIDER`、`LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`。
- 在回答记录中写入模型提供商、状态、失败原因和 fallback 信息。
- 明确至少一个多模态模型或多模态能力组合，用于满足赛题说明。
- 对语音识别、语音合成和数字人服务分别提供可替换配置。

验收方式：

- 没有 API Key 时系统仍可演示核心流程。
- 有 API Key 时可以切换真实模型。
- 模型失败不会造成接口崩溃或前端空白。

## 里程碑

### M1：稳定文本 MVP

- 修复前端中文乱码。
- 跑通后端测试和前端构建。
- 确认游客问答、知识库、记录、后台页面可用。
- 补充基础测试集和手动验收清单。

### M2：知识库与评测增强

- 完善知识分类和检索策略。
- 加入标准测试集与准确率统计。
- 加强低置信度兜底和来源引用展示。
- 管理端能查看知识库缺口。

### M3：游客端多模态与推荐

- 增加语音输入和语音播报。
- 增加兴趣标签和路线推荐。
- 优化游客端 UI，使演示链路更顺畅。

### M4：数字人与管理后台完善

- 稳定接入 LiveTalking 或替代数字人服务。
- 管理端增加数字人配置、游客报告和数据大屏 polish。
- 准备演示模式和失败兜底。

### M5：比赛交付材料

- 部署和使用手册。
- 总体设计文档。
- 方案介绍 PPT 大纲。
- 7 分钟演示视频脚本。
- 最终测试报告与风险说明。

## 讨论顺序建议

后续逐块讨论时，建议按这个顺序：

1. 知识库与 RAG 问答：决定准确率底盘。
2. 游客端体验：决定演示主线。
3. 数字人和语音链路：决定多模态亮点。
4. 个性化路线推荐：决定差异化能力。
5. 管理后台和游客洞察：决定行业实用性。
6. 交付材料和演示视频：决定比赛呈现。

## 当前优先问题

- 前端中文乱码必须优先修复，否则后续 UI 和演示都受影响。
- 需要确定真实模型、语音识别、语音合成和数字人服务的可用条件。
- 需要建立标准问题测试集，否则无法证明 90% 准确率。
- 需要保留离线演示方案，避免比赛现场依赖外部服务失败。
